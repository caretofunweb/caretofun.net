---
export interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className = '', narrow = false } = Astro.props;
const maxWidth = narrow ? 'max-w-4xl' : 'max-w-7xl';
---

<div class={`mx-auto px-4 sm:px-6 lg:px-8 w-full ${maxWidth} ${className}`}>
  <slot />
</div>
